// To be used when complex validation needs to be calculated
// Also used for when we want a popup type of notification for validation
export class ValidationResponse {
  result: boolean;
  message: string;
}
